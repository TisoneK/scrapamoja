description: "Incidents + Overtime sub-tab within STATS"
confidence_threshold: 0.9
timeout: 3.0
retry_count: 2
strategies:
  - type: "css"
    selector: "[data-subtab='inc-ot']"
    weight: 1.0
  - type: "css"
    selector: ".subtab-inc-ot"
    weight: 0.9
  - type: "xpath"
    selector: "//a[contains(text(), 'Inc OT') or contains(text(), 'Incidents')]"
    weight: 0.8
  - type: "css"
    selector: "[href*='inc-ot']"
    weight: 0.7
metadata:
  wait_for_element: true
  required_for_navigation: false
  tab_context: "stats"
  tertiary_context: "inc_ot"
