id: betting_odds
name: Betting Odds
selector_type: css
description: "Betting odds table rows (basketball)"
confidence_threshold: 0.7
timeout: 5.0
retry_count: 3

strategies:
  - type: "css"
    selector: ".ui-table__row"
    weight: 1.0
  - type: "css"
    selector: "[class*='ui-table__row']"
    weight: 0.9
  - type: "xpath"
    selector: "//*[contains(@class, 'ui-table__row')]"
    weight: 0.7

metadata:
  wait_for_element: true
  tab_context: "odds"
  stable_selectors:
    - ".ui-table__row"
  notes: "Odds table rows. Each row = one bookmaker with odds cells."
