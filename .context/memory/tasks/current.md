# Current Task — none

**Status:** idle — last session: 2026-09-08 Session 44 (Sam/S442; **quota monitor + auto-prune shipped**, `0398c94` + ADR-25: hourly scheduler pass reads the primary's `pg_database_size` — bypassing the ADR-24 fallback mirror — warns at 80% of `BETB2B_DB_LIMIT_MB` (500), auto-prunes fact history older than `BETB2B_PRUNE_DAYS` (7) at the 92% critical level; `events`/results always survive; CLI `quota` one-shot dry-run default; 4 new tests, suite **251 passed**). This ships the long-open ADR-22 retention pass. Collab: Alex (S443) ran a read-only secret-leak sweep, no overlap; notes on `collab/2026-09-08`. Committed + pushed.

**Supabase state (2026-09-08, end of day):** DB **1668 MB / 500 MB per-project (over 3.3×)** — dashboard read-only; project flagged for auto-pause (survivable: unpause within 90 days). Operator prune attempts via the dashboard SQL editor failed twice (25006 read-only; then an FK ordering error, fixed with the single-statement TRUNCATE, but the session-level SET override did **not** survive across editor runs — likely a transaction-mode pooler where session state doesn't persist). **OPERATOR DECISION 2026-09-08: STOP FORCING the prune.** Ride the restriction — the system is designed for it (ADR-24 fallback captures writes; scheduler backs off; auto-replay on recovery).

**OPERATOR PATH FORWARD (recorded 2026-09-08):**
- **Confirmed blocked via every dashboard surface** (SQL editor TRUNCATE, single-statement retry, **Table Editor row-delete — all 25006**): the read-only flag is enforced uniformly; no UI write path exists. Do not suggest dashboard write attempts again.
- **Nothing to do until on/after 2026-09-27** (org cycle reset) — optionally pause the Railway worker to save compute; leaving it running is also fine (backoff is graceful).
- **Path A (keep the project):** unpause if paused → ONE retry of the prune with the override bound *inside the transaction* (immune to pooling): `BEGIN; SET TRANSACTION READ WRITE; TRUNCATE odds_snapshots, scrape_runs, event_states, period_scores, h2h_games, h2h_period_scores, statistics, sub_games; COMMIT;`. If still 25006 → Path B. Note: the size-based project read-only does NOT lift on its own while at 1.67 GB — the cycle reset alone is not a fix.
- **Path B (guaranteed, ~30 min, the August fresh-start playbook generalized):** delete + recreate the Supabase project (fresh 500 MB), update `DATABASE_URL` on BOTH Railway services, redeploy — the store re-inits its schema automatically. Accepted cost: the current store's history (the operator accepted the same class of cost on 2026-08-05).
- **⚠️ Redeploy caveat meanwhile:** the fallback mirror + outbox live on the worker's ephemeral disk unless `BETB2B_FALLBACK_DB_PATH` points at a Railway Volume. A worker redeploy before recovery resets the queued data (replaceable by re-scrape; scheduled-only inflow is small). The pending redeploy (live-off fix + quota monitor) therefore resets it — acceptable, or attach a Volume first.
- Supabase auto-pause in the meantime is harmless: unpause from the dashboard whenever resuming (within 90 days).

**Next up (tasks/backlog.md):** ADR-20 addendum results-fetch capture (HIGH when writable); ADR-23 in-process last-odds cache before any live re-enable on a metered plan; egress monitoring only if it binds again. Operator direction recorded in user/preferences.md: stay free-tier now, graduate to paid/bigger hosting once the system proves out (config-level graduation: `BETB2B_DB_LIMIT_MB` up, `SCHED_LIVE_INTERVAL=15`, ADR-23 cache first).
