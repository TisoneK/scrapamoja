# Session 48 Extension Review — Context Ledger Migration (Miles/S446, 2026-09-14)

## Executive Summary

Operator issued the go ("Migrate") for the pending core MAJOR. This
extension migrated the repo **0.17.0 → 1.1.1 → 1.1.2** via the documented
Context Ledger dance — `context-sync update -Major` (core swap) →
`ledger-sync migrate` (zone backfill + the 1.0.0 **office regroup** +
relock) → `ledger-sync rename` (`.context/` → `.context_ledger/`, tools
`context-*` → `ledger-*`, entry-point sweep) — then applied the
same-day PATCH 1.1.2 and ran the prescribed `ledger-mem closeout`
(23 tombstones left the backlog; open work only now). Product code
untouched; suite **253 passed / exit 0** before and after (final run
post-1.1.2). 18 commits pushed; 4 follow-up commits in the wrap round.

## What 1.1.1/1.1.2 changed for agents (binds the next session)

- **Memory layout:** session-produced files live in the live office —
  `memory/office/{agents,tasks,plans,reviews,sessions,flaws,inefficiencies}`;
  durable files (workflows, collaboration, system, user, overrides,
  secrets, core.lock) at the memory root. `history/` freezes closed
  offices; current office: 7/20 sessions (no close due).
- **Tools:** everything is `ledger-*`; Windows `.cmd` launchers are the
  standard (the PS-port override is retired).
- **Check-in is the door:** sign the roster (now with **Status** +
  **Status detail** cells) before the deep read; the push claims the
  codename; after clock-out + follow-up work you **check back in with
  the same name + codename and extend the same session entry** (this
  session did exactly that — S447 check-in was a pre-migration mistake,
  corrected in `90c460d`).
- **Memory commits now use `chore(ledger):`** (was `chore(context):`).
- **Backlog = live queue**; logs **compact** (RESOLVED → `archive.md`,
  recurring roll-ups); **one-way linkage enforced** — `ledger-mem lint
  --tree` sweeps product files for `.context_ledger` vocabulary, leaks
  stripped on sight (first full-tree sweep due next product session;
  staged-diff lint passed every commit here).
- 1.1.2 (PATCH): `ledger-mem prune` now reads closed markers only on an
  entry's own `**Status:**` line — compaction advice trustworthy.

## Migration observations (traps hit, all handled)

1. `context-sync update` refuses a cross-MAJOR bump without `-Major` —
   expected; operator go was on record.
2. Post-swap self-re-exec error of the old tool is documented-expected
   (MIGRATION.md) — core was already 1.1.1.
3. The rename's entry-point sweep is a **dumb text replace**: it rewrote
   historical package names (`TisoneK/.context` → `TisoneK/.context_ledger`
   — a name that never existed) and left old *tool* names
   (`context-sync verify`) inside the swept files. Fixed by regenerating
   kickoff/CLAUDE/collab/flaws-README from templates and re-merging the
   AGENTS digest; `MIGRATION.md`'s manual step ("sweep stale
   instruction references") is mandatory, not optional.
4. My own sed refill mangled a Windows path (`\U` = GNU sed upcase
   directive): verify mechanical replacements with a diff (`aec0717`).
5. `ledger-sync status` resolved the source to the operator's working
   clone at `..\context-ledger` (auto after their self-host commits);
   drift check done against a freshly-pulled clean clone instead
   (`C:\Users\Lameck\Tisone\.context`, HEAD `0714a2f`, 1.1.2).
6. `ledger-mem check` warns on empty roster Status cells (warn, never
   fails) — the corrected row carries them.
7. Dependabot still 16 (4 high, 11 moderate, 1 low) — remote notices on
   every push confirmed the count live.

## Verification (evidence)

- `ledger-sync verify` → "core OK: every file matches MANIFEST.sha256
  and every port parses (1.1.2)"; lock = 1.1.2.
- `ledger-mem check` → no duplicate keys. `ledger-history status` →
  office 7/20, 0 frozen offices.
- `ledger-gates checkpoint` ×N passed; pre-commit universal checks
  passed every commit (configured POSIX pytest command still fails 127
  — known trap — manual equivalent is the accepted evidence).
- Suite: `.venv/Scripts/python.exe -m pytest src/sites/betb2b/tests/
  --no-cov -q -p no:cacheprovider` → **exit 0, 253 dots, 0 F/E** (post
  1.1.2, final run this session).

## Commits

`bdee442`…`845b49e` — 18 commits, full trail in `git log bdee442~1..HEAD`
(check-in, core swap, regroup, rename, kickoff/AGENTS/CLAUDE refresh +
2 repairs, roster correction, 4 reference sweeps, environments registry,
flaws README, 1.1.2 update, backlog closeout, active). Wrap commits
(report + memory extension + clock-out) land after this file.

## Open items

None new — plus one next-session courtesy: first **product** commit
should run `ledger-mem lint --tree` (the new whole-tree leak sweep) and
strip anything found. Supabase cycle-reset watch unchanged (2026-09-27);
ADR-20 addendum, ADR-23 cache, Dependabot triage, secret-scanner gate,
history-rewrite decision all still open in `office/tasks/backlog.md`.
